make wildcard look for UNcasesensitive.
handle echo $? in parser to expand to last command
export 1VAR=test (cant take number before)
export doesnt take trailing UNDERSCORE (tested in bash in mac, this seems to be accepted.)

[exit case to print
jotong@c2r4s2:~/minishell$ exit abc
exit
bash: exit: abc: numeric argument required
SET EXIT CODE]
[DONE 14 dec]
 
[print error of
jotong@c2r4s2 ~/minishell % | cat
zsh: parse error near `|']

ls 2>err.txt >out.txt (NEED TO FIX SO REDIR TAKES FROM CORRECT OUT)

cannot do ctrl + C inside heredoc, it should exit out of heredoc

echo -xxxn whatever flag that contain n should flip the n flag, print the rest like text
throw error if other flags are provided.
[update 14 dec: double checked on my bash, any flags that contain other flags than n just ends up printing it as a string -- no change needed.]

all other builtins: handle without flags. throw an error

cd .. should fail after removing the parent folder
(note:
cd without arguments goes to $HOME
This should work because it's an absolute path resolution)
[update 17 Dec: fixed]

the following commands can
ls ./
ls ../
cat ./somefile.txt

pwd not updated after cd into folder
in the case of unsuccessful cd, the pwd should not be updated.
[update 17 dec: fixed]

when echo $R (when R="      test      ")   <-- do inside echo function
need to remove the spaces before
but echo test$R returns:
test   test


Either of the following cases currently dont return any output:
ls|grep test
ls|grep test|wc -l      <-- if "test" exists in the file, it will return 1, otherwise 0


echo $USER|cat  (no spaces)                     <-- tokeniser error
the above should return the name of the user
[fixed]

echo &&& : curently hangs (program doesnt exit). expected error:
bash: syntax error near unexpected token `&'

echo $USER&&echo test
need to stop parsing for variable (and create a new token) when i stop seeing alphanumeric or _


cd " " should return an error
[14 dec: this was already handled.]

echo a&&echo b should print:
a
b
[fixed]

HEREDOC needs a separate expansion logic to fit:
- tilde
- double quotes
- single quotes
- *
- " SINGLE DOUBLE QUOTES PRINT OUT.

rechaining FDS are causing errors when first redirect is invalid

EXPECTED BEHAVIOUR
- echo $_ currently prints the pwd and program names
- echo $# current prints $#
- echo ${} or echo ${ } prints out the explicit statement